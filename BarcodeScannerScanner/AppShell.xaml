<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="BarcodeScannerScanner.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:BarcodeScannerScanner"
    Shell.FlyoutBehavior="Disabled"
    Title="BarcodeScannerScanner">

    <!--<ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />-->
    <FlyoutItem Title="Dashboard" x:Name="myflyout" FlyoutDisplayOptions="AsSingleItem">
        <Tab Title="Home" >
            <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:HomePage}"
        Route="HomePage" />
        </Tab>


        <Tab Title="Scan">
            <ShellContent 
        Title="Scan"  Shell.NavBarIsVisible="False"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
        </Tab>
    </FlyoutItem>


</Shell>

